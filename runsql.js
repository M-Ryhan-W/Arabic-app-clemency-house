import dotenv from "dotenv"; dotenv.config({path: ".env.local"}); import { createClient } from "@supabase/supabase-js"; import fs from "fs"; const sql = fs.readFileSync("user_daily_stats.sql").toString(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { console.log(await supabase.rpc("exec_sql", { query: sql })); } run();
